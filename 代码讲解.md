# 计算器代码讲解（写给编程小白）

这份文档配合三个文件一起看，建议**左边开代码、右边开这份文档**对照着读。

---

## 一、先搞清楚三个文件的分工

| 文件 | 负责什么 | 类比 |
| --- | --- | --- |
| `index.html` | 页面上**有哪些东西**（按钮、标题、显示屏的位置） | 房子的**毛坯结构**：哪里是墙、哪里是门 |
| `style.css` | 这些东西**长什么样**（颜色、大小、间距、动画） | **装修**：刷什么颜色的漆、铺什么地板 |
| `script.js` | 点下去之后**发生什么**（计算、显示、保存历史） | **水电和开关**：按一下灯就亮 |

三者通过 **class 名** 和 **id** 互相"接头"：

```
index.html 的 <div id="result">  ←──  style.css 用 .display__result 给它上色
                                  ←──  script.js 用 getElementById("result") 往里写数字
```

> 记住一句话：**HTML 描述结构，CSS 描述外观，JS 描述行为。**

---

## 二、最重要的概念：`state`（状态）

打开 `script.js`，第 33 行左右有个 `state` 对象：

```js
const state = {
  tokens: [],            // 用户按出来的记号序列
  history: [],           // 历史记录
  justEvaluated: false,  // 刚刚是否按过等号
};
```

**整个计算器就是围绕这三个值在运转。** 界面长什么样，完全取决于 `state` 里存了什么。

### `tokens` 是关键中的关键

它不是普通的字符串，而是**一个数组，每个元素是一个完整的"记号"**：

| 你按下 | `state.tokens` 变成 | 屏幕上显示 |
| --- | --- | --- |
| `1` | `["1"]` | 1 |
| `2` | `["12"]` | 12 ← 注意：合并成一个元素了！ |
| `×` | `["12", "×"]` | 12× |
| `sin(` | `["12", "×", "sin("]` | 12×sin( |
| `π` | `["12", "×", "sin(", "π"]` | 12×sin(π |
| `)` | `["12", "×", "sin(", "π", ")"]` | 12×sin(π) |

**为什么不用一个长字符串 `"12×sin(π)"`？**

因为退格会变得很难处理。如果用字符串，删一次只能删掉一个字符 `)`、`π`、`(`、`n`、`i`、`s`…… 要连按 4 次才能删掉一个 `sin(`。
而用数组，每个记号是一个整体，`pop()` 一次就干净地删掉了 `"sin("`。

> 这是编程里非常常见的思想：**选择合适的数据结构，能让后续的逻辑变得简单。**

---

## 三、一次点击的完整旅程

我们跟着"点击数字 7"这个动作，走一遍全流程：

```
① 用户点击屏幕上的 [7] 按钮
        ↓
② 触发 click 事件，被 .keys 上的监听器接住
   （这叫"事件委托"，只绑一个监听器就能管 30 个按钮）
        ↓
③ event.target.closest(".key") 找到被点的按钮
        ↓
④ 读取按钮上的自定义属性：
   button.dataset.value → "7"
        ↓
⑤ 调用 insert("7")
        ↓
⑥ insert 内部修改 state：
   state.tokens 从 [] 变成 ["7"]
        ↓
⑦ 调用 render()
        ↓
⑧ render 读出 state.tokens，拼成字符串 "7"
   写进屏幕：exprEl.textContent = "7"
        ↓
⑨ 同时试着算一遍，把结果写进 resultEl
```

**这条链条最值得记住的是第 ⑥⑦ 步的分工：**
> 函数只负责**改数据**，改完叫一声 `render()`，由 `render` 统一负责**改界面**。

这种做法叫「数据驱动视图」。好处是：永远不会出现"屏幕显示的和实际算的对不上"的情况。

### 按等号时的旅程

按下 `=` 会走一条更长的路：

```
equals()
  → evaluateTokens(state.tokens)      先把 "12×sin(π)" 算成 -0.0000000000000001…
      → tokenize()                    第一步：翻译成 [{type:"num",value:12}, {type:"op",op:"×"}, …]
      → parseExpression() 等一串函数   第二步：按优先级真正计算
  → formatNumber()                    第三步：把 -1e-16 美化成 "0"
  → 存进 state.history + localStorage
  → state.tokens = ["0"]              结果本身成为一个新记号，可以接着算
  → render() + renderHistory()
```

---

## 四、最难的部分：解析器是怎么工作的

`evaluateTokens` 里那一堆 `parseXxx` 函数，是整个项目最"烧脑"的地方，但它背后的想法其实很简单。

### 要解决的唯一问题：优先级

`3 + 4 × 2` 必须等于 **11**，而不是 **14**。可计算机是从左往右读的，它怎么知道先算乘法？

### 解决办法：把优先级写成"函数的层次"

```
parseExpression  只处理 +  −        ← 最后算，写在最外层
      └─ parseTerm      只处理 ×  ÷ 和隐式乘法
             └─ parseUnary   只处理正负号
                    └─ parsePower    只处理 ^
                           └─ parsePostfix  只处理 % ²
                                  └─ parsePrimary  数字、括号、函数  ← 最先算，写在最里层
```

**越先算的运算，函数写得越靠里。** 因为外层函数必须等内层函数把值算完才能继续。

### 实际走一遍 `3 + 4 × 2`

```
parseExpression 开始
  ├─ parseTerm → 拿到 3
  ├─ 看到 + 号，吃掉它
  └─ 再要一个 parseTerm（右边的东西）：
        ├─ parseTerm 拿到 4
        ├─ 看到 × 号，吃掉它
        └─ 再要一个 parseUnary → 拿到 2
           算出 4 × 2 = 8  ← 乘法在这里就先算完了
     返回 8
  最后：3 + 8 = 11  ✅
```

**这就是"递归下降解析器"**，编译器就是用它来读懂你的源代码的。能自己写出这个，说明你已经入门了。

### 为什么不用 `eval`？

`eval("3+4*2")` 一行就能算完，看起来省事，但：

1. **危险**：`eval` 会把字符串当代码执行。如果字符串来自用户输入，用户就能执行任意代码（比如偷数据、删文件）。
2. **不够用**：`eval` 看不懂 `×` `÷` `π` `²`，还得先做替换，反而更乱。

所以宁可自己写 100 行解析器，也不用 `eval`。**这是编程中的一个重要原则：能明确控制的代码，优于"神奇但危险"的捷径。**

---

## 五、几个"为什么这么写"的知识点

### 1. 为什么 `0.1 + 0.2` 不等于 `0.3`？

在浏览器控制台试试 `0.1 + 0.2`，你会看到 `0.30000000000000004`。

原因：计算机用**二进制**存小数，而 0.1 换算成二进制是无限循环小数（就像十进制写不出 1/3），只能近似存储，误差就产生了。

**这不是 JS 的 bug，是所有编程语言都有的问题。** 我们的处理办法在 `formatNumber()` 里：

```js
String(parseFloat(value.toFixed(10)))
```

`toFixed(10)` 只保留 10 位小数，把没用的小尾巴砍掉，再转回数字，就得到干净的 `0.3`。

### 2. 为什么键盘上的 `-` 和屏幕上的 `−` 不一样？

它们是两个不同的字符：

- 键盘上的 `-`：叫 hyphen（连字符），短
- 屏幕上的 `−`：叫 minus（数学减号），长且居中，更好看

所以代码里有 `KEYBOARD_MAP` 把键盘按键翻译成界面使用的符号。另外负数 `-5` 里的负号也是 `−`，存成 `"−5"` 这样"负号贴在数字内部"，就不会和作为运算符的减号搞混。

### 3. `localStorage` 是什么？

浏览器给每个网站提供的一个小仓库（大约 5MB），**关掉浏览器数据也还在**。只能存字符串，所以：

```js
localStorage.setItem("键", JSON.stringify(数组))  // 存：数组 → 字符串
JSON.parse(localStorage.getItem("键"))            // 取：字符串 → 数组
```

我们用它在两个地方：

- `calculator.theme` → 记住你选的主题
- `calculator.history` → 记住历史记录

### 4. `try / catch` 是干什么的？

```js
try {
  // 可能出错的代码
} catch (err) {
  // 出错时怎么办
}
```

计算器非常依赖它。因为用户随时可能输入 `12+` 这种"半截算式"，`evaluateTokens` 会主动 `throw new Error(...)` 报错。如果不接住，页面就会停下来，用户看到一片空白。

**用 `try/catch` 把"会失败的操作"包住，是一种很常规的写法。** 我们的策略是：算不出来就静默跳过，等用户输入完整再算。

### 5. 为什么监听器要绑在 `.keys` 上，而不是每个按钮上？

```js
keysEl.addEventListener("click", ...)   // ✅ 只绑 1 次
```

30 个按钮如果各绑一次，就要写 30 次代码。绑在父元素上利用事件**冒泡**（点击会从子元素往上传递），只需写一次，这叫**事件委托**。

好处：以后动态新增的按钮也自动生效，不用重新绑定。

---

## 六、动手改一改（最好的学习方式）

建议每改一次就刷新页面看效果。**改坏了也没关系，撤销就好。**

### 练习 1：换个配色（练 CSS 变量）

打开 `style.css`，找到最上面的 `:root { --brass: #a2743a; }`，把 `--brass` 改成 `#2e6b5e`（墨绿），保存刷新——所有用黄铜色的地方**一次性全变了**。

> 这就是 CSS 变量的威力：改一处，全局生效。

### 练习 2：把按键换成圆形的（练 CSS）

在 `style.css` 找到 `.key`，把 `border-radius: 9px;` 改成 `50%;`，按键就全变成圆形了。

### 练习 3：新增一个按键（练 HTML + JS 联动）

假如想加一个"阶乘"按钮 `n!`。先在 `index.html` 的按键区随便找个位置加一行：

```html
<button class="key key--soft" type="button" data-value="!">n!</button>
```

它现在还不会工作，因为 JS 不认识 `!`。接着在 `script.js` 里做三处登记：

1. `POSTFIX` 数组里加上 `"!"`
2. `insert()` 里 `value === "%" || value === "²"` 那个判断改成也包含 `"!"`
3. `parsePostfix()` 里计算阶乘

```js
value = t.op === "%" ? value / 100 : value * value;
// 改成三目嵌套或 if/else，给 "!" 加一个分支
```

### 练习 4：调整历史记录条数

`script.js` 里有两处 `slice(0, 50)`，把 50 改成 10，就只保留最近 10 条。

---

## 七、怎么自己调试（重要技能）

在浏览器里按 **F12** 打开开发者工具：

| 面板 | 用途 |
| --- | --- |
| **Console（控制台）** | 看报错信息，也可以直接敲代码测试 |
| **Elements（元素）** | 看网页实际生成的结构，鼠标悬停能看到每一块的范围 |
| **Sources（源代码）** | 打断点，让代码"暂停"下来一步步执行 |

### 最实用的调试手段：`console.log`

在 `render()` 函数里加一行：

```js
function render() {
  console.log("当前 tokens：", state.tokens);   // ← 加这一行
  exprEl.textContent = state.tokens.join("");
```

刷新页面后，你每按一个键就会在控制台看到 `state.tokens` 的变化。**看数据是怎么一步步变化的，比盯着代码空想有效 10 倍。**

调试完记得把这行删掉。

### 如果按键没反应，按这个顺序排查

1. 打开 Console，**有没有红色报错**？报错信息通常会告诉你在第几行。
2. 加 `console.log` 看**事件到底有没有触发**。
3. 检查 HTML 里的 `id` / `class` 和 JS 里写的是否**完全一致**（大小写敏感！`btn` 和 `Btn` 是两个东西）。

---

## 八、哈希计算是怎么实现的

### 1. 哈希是什么

一句话：**把任意长度的内容，压缩成固定长度的一串字符**（叫"摘要"或"指纹"）。

| 算法 | 输出长度 | 十六进制字符数 | 现状 |
| --- | --- | --- | --- |
| MD5 | 128 位 | 32 个 | 早已被破解，**不能**用来存密码；但做校验码、内容指纹仍很常用 |
| SHA-1 | 160 位 | 40 个 | 已被证明不安全，新项目不建议使用 |
| SHA-256 | 256 位 | 64 个 | 目前安全，应用最广（区块链、网站证书都在用） |
| SHA-512 | 512 位 | 128 个 | 目前安全，位数越多越难被撞库 |

三个重要特点：

1. **单向**：只能从原文算出哈希，无法从哈希反推原文
2. **定长**：输入一个字还是一整本书，输出长度都一样
3. **雪崩**：原文改一个字，结果面目全非

### 2. 为什么两条路：一个自己写、一个交给浏览器

- **SHA 系列**：浏览器内置了 `crypto.subtle`（叫 Web Crypto API），直接用。它的计算由浏览器底层实现，比 JS 手写快得多。
- **MD5**：浏览器没有提供，只能自己用 JS 实现一遍（`script.js` 里的 `md5` 函数）。

自己写 MD5 的过程分四步：

```
① 补位   把原文补齐到 64 字节的整数倍（补一个 0x80，再补 0，最后 8 字节存原长度）
② 初始化 准备 4 个 32 位整数 A、B、C、D（标准里写死的初始值）
③ 主循环 每 64 字节一组，做 64 轮「位运算搅拌」，不断翻搅 A B C D
④ 输出   把 A B C D 拼成 32 个十六进制字符
```

看不懂每一轮的具体位运算完全正常 —— 关键是理解它在**反复搅拌**：
改动一个比特，最终结果会有大约一半的比特跟着翻转，这就是"雪崩"。

### 3. 一个容易被忽略的坑：异步"抢跑"

SHA 的计算是异步的（要等浏览器返回结果）。假如用户手速很快：

```
第 1 次：输入 "a"    → 开始计算（慢了）
第 2 次：输入 "ab"   → 开始计算（很快返回，屏幕显示 "ab" 的哈希）
        → 第 1 次的结果这时才回来，把 "ab" 的结果覆盖掉了 ❌
```

屏幕上显示的就是**过期的错误结果**。

代码里的解决方式叫"编号防抢跑"，只有三步：

```js
const taskId = (hashTaskId += 1);   // 每开始一次计算，先领一个递增的号
value = await computeHash(...);     // 等结果
if (taskId !== hashTaskId) return;  // 号对不上 → 说明有更新的计算，直接丢弃
```

**这个思路在做搜索框联想、表单实时校验时都会用到**，很值得记住。

### 4. 怎么用这个功能

1. 点右上角的 `#` 按钮打开面板
2. 在文本框里输入任意内容（中文 / 英文 / 数字都行），结果实时更新
3. 切换 MD5 / SHA-1 / SHA-256 / SHA-512 查看不同算法的结果
4. 点「复制」把结果复制到剪贴板
5. 按 `Esc`、点 ✕、或点面板外的暗色区域都能关闭

> 注意：SHA 依赖浏览器的 Web Crypto API，它要求"安全环境"。
> 用 `https://`、`localhost`，或者直接双击打开（`file://`）都可以用。
> 如果看到"当前环境不支持该算法"的提示，就先换成 MD5 试试。

---

## 九、这个项目用到的知识点清单

如果你想系统地学，可以按这个顺序：

- [ ] **HTML**：标签嵌套、`class` / `id`、语义标签（`header` / `main` / `aside`）、自定义属性 `data-*`
- [ ] **CSS 基础**：选择器、盒模型（`box-sizing`）、颜色单位（`rgba` / 十六进制）
- [ ] **CSS 布局**：`flex`（一维排列）、`grid`（二维网格）、`position` 定位
- [ ] **CSS 进阶**：CSS 变量、伪元素 `::before`、`transition` 过渡、`@keyframes` 动画、媒体查询 `@media`
- [ ] **JS 基础**：变量 `const` / `let`、数组和对象、函数与箭头函数、`if` / `for` / `while`、模板字符串
- [ ] **JS 数组方法**：`map` / `filter` / `forEach` / `push` / `pop` / `slice` / `join`
- [ ] **JS DOM**：`getElementById` / `querySelector` / `textContent` / `classList` / `createElement`
- [ ] **JS 事件**：`addEventListener`、事件对象 `event`、事件委托、`preventDefault`
- [ ] **JS 进阶**：`try/catch` 错误处理、`localStorage`、`JSON`、闭包（`flash` 里的 `flashTimer`）、递归（解析器）
- [ ] **算法**：递归下降解析、运算符优先级

**还看不懂哪一段，就直接问。** 指着具体的函数名和行号问，我能给你讲得更细。
